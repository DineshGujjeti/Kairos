# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Config & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Database (PostgreSQL) ---
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
alembic==1.13.3

# --- Database (DuckDB analytical store) ---
duckdb==1.1.1

# --- Ingestion pipeline (Module 2) ---
pandas==2.2.3
openpyxl==3.1.5      # .xlsx read support for pandas
xlrd==2.0.1           # legacy .xls read support for pandas
pandera==0.20.4       # structural dataset validation

# --- Auth / security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pinned: passlib 1.7.4 reads bcrypt.__about__, removed in bcrypt>=4.1
python-multipart==0.0.9

# --- Logging ---
structlog==24.4.0

# --- AI/Gemini (Module 6) ---
google-genai>=0.4.0

# --- Root Cause Intelligence (Module 7) ---
scikit-learn>=1.3.0

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2

# --- Dev tooling ---
black==24.8.0
ruff==0.6.9
mypy==1.11.2

# NOTE: scikit-learn, lightgbm, xgboost, prophet, shap, langgraph are
# intentionally NOT included yet -- they belong to the ML/Forecasting/
# Explainability/Assistant modules and will be added when those modules
# are built, so Module 1's image stays small and installs fast.
